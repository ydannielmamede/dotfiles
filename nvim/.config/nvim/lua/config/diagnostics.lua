vim.diagnostic.config({
  virtual_text = true,   -- mostra erro ao lado da linha
  signs = true,          -- mostra ícone na coluna lateral
  underline = true,      -- sublinha o erro
  update_in_insert = false,
  severity_sort = true,
})
